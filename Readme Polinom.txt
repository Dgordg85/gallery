ѕолином
========

modString
‘унци€ подключает другие функции обработчики строки.

check
ѕровер€ет строку на количество скобок - на правильность операций (две подр€д), на присутствие операции делени€.

cString
ѕреобразует строку дл€ дальнейшкей работы - мен€ет скобочки на валидные, переменные приводит к единому виду х, раставл€ет знаким умножени€ пропущенные, убирает пробелы, знак равно.

transformation
«амен€ет переменные в строке на номера /1/ - где 1 €вл€етс€ 1-ым элементом массива, где хранитьс€ его значение. “ребуетс€ дл€ преобразовани€ в обратную польскую запись.

rpn
ѕереводим строку в обратную польскую запись, чтобы избавитьс€ от скобок.

outRpn
ќбрабатываем обратную польскую запись, использую массив со значени€ми от transformation.

finalTransformation
ѕреобразует число к естественному виду - складывает, что с чем можно сложить и выводит начина€ со старшей степени переменной x


¬спомогательные функции дл€ OutRpn
==================

openBrackets
ѕеремножает несколько значений (A+B)*(—+D)

additionNumbers
вычитает/складывает два числа вида -5x^7; 8x^2 - если это возможно

multiplicationNumber
умножает два числа между собой


¬спомогательна€ функци€ дл€ additionNumbers openBrackets
==================
stringToArr
¬спомогательна€ функци€, разбивающа€ строку на элементы
